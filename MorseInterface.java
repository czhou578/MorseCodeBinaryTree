public interface MorseInterface {

    void inOrderPrint(); //prints the tree 
    String decode(String str); //method decodes the string of morse code into text
    String encode(String str); //method encodes text into morse code

}